# ore = copper
# stopAt = 20

# getNormalLink()
# print(building)
# printflush()

# set('linkCounter', 0)
# getlink(var, 'linkCounter')
# sensor('buildingType', var, '@type')
# jump(-3, f"buildingType == @{type}")


linkCounter = 0
dowhile(buildingType == type){
    getlink(building, linkCounter)
    sensor(buildingType, building, type)
    increment(linkCounter)
}
sensor(count, building, totalItems)
printf(count)